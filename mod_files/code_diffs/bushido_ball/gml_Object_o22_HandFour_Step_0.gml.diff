*** original_scripts/bushido_ball/gml_Object_o22_HandFour_Step_0.gml	Thu Jan  1 00:00:00 1970
--- ufo50_modded_scripts/bushido_ball/gml_Object_o22_HandFour_Step_0.gml	Thu Oct 16 08:11:46 2025
***************
*** 0 ****
--- 1,174 ----
+ if (!active)
+     exit;
+ 
+ if (!ai)
+ {
+     scrGetInput(o22_Game.controllerMap[3]);
+     
+     if (readyCount > 10)
+         readyCount = 10;
+ }
+ else
+ {
+     scrInputClear();
+     
+     if (moveCount > 0)
+     {
+         moveCount -= 1;
+     }
+     else
+     {
+         moveCount = moveCountMax;
+         
+         if (sel == 0)
+         {
+             n = rand(1, 2);
+             
+             if (n == 1)
+                 pressDown = true;
+             else if (n == 2)
+                 pressRight = true;
+         }
+         else if (sel == 1)
+         {
+             n = rand(1, 3);
+             
+             if (n == 1)
+                 pressDown = true;
+             else if (n == 2)
+                 pressLeft = true;
+             else if (n == 3)
+                 pressRight = true;
+         }
+         else if (sel == 2)
+         {
+             n = rand(1, 2);
+             
+             if (n == 1)
+                 pressDown = true;
+             else if (n == 2)
+                 pressLeft = true;
+         }
+         else if (sel == 3)
+         {
+             n = rand(1, 2);
+             
+             if (n == 1)
+                 pressUp = true;
+             else if (n == 2)
+                 pressRight = true;
+         }
+         else if (sel == 4)
+         {
+             n = rand(1, 3);
+             
+             if (n == 1)
+                 pressUp = true;
+             else if (n == 2)
+                 pressLeft = true;
+             else if (n == 3)
+                 pressRight = true;
+         }
+         else if (sel == 5)
+         {
+             n = rand(1, 2);
+             
+             if (n == 1)
+                 pressUp = true;
+             else if (n == 2)
+                 pressLeft = true;
+         }
+     }
+ }
+ 
+ if (!ready)
+ {
+     var pSel = sel;
+     
+     if (pressUp)
+     {
+         if (sel > 2)
+             sel -= 3;
+     }
+     else if (pressDown)
+     {
+         if (sel < 3)
+             sel += 3;
+     }
+     else if (pressLeft)
+     {
+         if (sel != 0 && sel != 3)
+             sel--;
+     }
+     else if (pressRight)
+     {
+         if (sel != 2 && sel != 5)
+             sel++;
+     }
+     
+     if (sel != pSel)
+         scrSfx(sfx_navi06, 50);
+     
+     //o22_CharselRight.val = sel;
+     //o22_PortraitRight.val = sel;
+     
+     switch (sel)
+     {
+         case 0:
+             x = camera_get_view_x(view_get_camera(0)) + 112 + 32;
+             y = camera_get_view_y(view_get_camera(0)) + 64 + 16;
+             break;
+         
+         case 1:
+             x = camera_get_view_x(view_get_camera(0)) + 112 + 32 + 48;
+             y = camera_get_view_y(view_get_camera(0)) + 64 + 16;
+             break;
+         
+         case 2:
+             x = camera_get_view_x(view_get_camera(0)) + 112 + 32 + 96;
+             y = camera_get_view_y(view_get_camera(0)) + 64 + 16;
+             break;
+         
+         case 3:
+             x = camera_get_view_x(view_get_camera(0)) + 112 + 32;
+             y = camera_get_view_y(view_get_camera(0)) + 112 + 16;
+             break;
+         
+         case 4:
+             x = camera_get_view_x(view_get_camera(0)) + 112 + 32 + 48;
+             y = camera_get_view_y(view_get_camera(0)) + 112 + 16;
+             break;
+         
+         case 5:
+             x = camera_get_view_x(view_get_camera(0)) + 112 + 32 + 96;
+             y = camera_get_view_y(view_get_camera(0)) + 112 + 16;
+             break;
+     }
+     
+     if (ai)
+     {
+         if (readyCount > 0)
+             readyCount -= 1;
+         else if (o22_Game.charsDefeated[sel] == 0 && sel != o22_Game.p4_sel)
+             event_user(0);
+     }
+     else if (readyCount > 0)
+     {
+         readyCount -= 1;
+     }
+     else if (fire2pressed)
+     {
+         event_user(0);
+     }
+ }
+ else if (!ai)
+ {
+     var _canBackOut = !o22_HandLeft.ready || !o22_HandRight.ready || !o22_HandThree.ready;
+     
+     if (_canBackOut && fire1pressed)
+     {
+         ready = false;
+         readyCount = readyCountMax;
+         o22_Game.p4_sel = -1;
+     }
+ }
